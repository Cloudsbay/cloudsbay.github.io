---
---

<div id="background"></div>

<div id="container">
	<main>
		<section id="hero">
			<h1>Welcome to Cloudsbay</h1>
		</section>
	</main>
</div>

<style>
	:root {
		--primary-color: #F21905;
		--surface-shadow: rgba(0, 0, 0, 0.4);

		--tremendous-size: 200px;
		--tremendous-line-height: 1.1;
		--tremendous-weight: 800;
	}

	#background {
		position: fixed;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		z-index: -1;
		filter: blur(120px);
		background: radial-gradient(circle at center, #1a1a1a, #000);
	}

	#container {
		font-family: 'Roboto', 'Inter', 'Helvetica Neue', 'Arial Nova', 'Nimbus Sans', Arial, sans-serif;
		min-height: 100vh;
		margin: 0;
		padding: 0;
		box-sizing: border-box;
		background-color: #000;
		color: var(--primary-color);
	}

	main {
		display: flex;
		justify-content: flex-start;
		align-items: flex-start;
		padding: 4rem;
		height: 100%;
	}

	#hero {
		width: 100%;
	}

	h1 {
		font-size: var(--tremendous-size);
		line-height: var(--tremendous-line-height);
		font-weight: var(--tremendous-weight);
		margin: 0;
		letter-spacing: -1.5px;
		word-break: break-word;
		color: var(--primary-color);
	}

	@media (max-width: 768px) {
		h1 {
			font-size: 48px;
			line-height: 1.2;
		}
	}
</style>